# Discord bot token (required)
# Create a bot at https://discord.com/developers/applications
DISCORD_TOKEN=your-bot-token-here

# OpenRouter API key (required for insight commands)
# Get one at https://openrouter.ai/keys
OPENROUTER_API_KEY=your-openrouter-key-here

# GitHub token for public gist publishing used by /audit
# Use a token from the journalnode account with gist write access
# GITHUB_GIST_TOKEN=your-github-token-here

# Optional: override the default LLM model
# OPENROUTER_MODEL=anthropic/claude-opus-4.6

# Optional: restrict the bot to a single channel by ID
# JOURNAL_CHANNEL_ID=123456789012345678

# Post Fiat testnet — master wallet seed for airdropping to new users (required for /postfiat)
# PFT_MASTER_SEED=sYourMasterSeedHere

# Optional: override the default airdrop amount (default: 50 PFT)
# PFT_AIRDROP_AMOUNT=50

# Optional: Pinata JWT for uploading images to IPFS via /mint command
# Get one at https://app.pinata.cloud/developers/api-keys
# PINATA_JWT=your-pinata-jwt-here

# Task Node audit agent seed file (recommended for production)
# Create a dedicated bot wallet and point this at a local seed file.
# Example: TASKNODE_AGENT_SEED_FILE=data/tasknode-agent/seed.txt
# TASKNODE_AGENT_SEED_FILE=

# Optional: override the Task Node audit model (defaults to openai/gpt-5.4)
# TASKNODE_AGENT_AUDIT_MODEL=openai/gpt-5.4

# Optional: Task Node inbound polling interval in milliseconds (default: 15000)
# TASKNODE_AGENT_POLL_INTERVAL_MS=15000

# Optional: set to true to process existing inbound messages on first boot
# TASKNODE_AGENT_PROCESS_EXISTING=false
